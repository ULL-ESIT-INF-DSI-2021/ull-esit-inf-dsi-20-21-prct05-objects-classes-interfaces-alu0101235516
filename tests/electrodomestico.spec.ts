/*
import 'mocha';
import {expect} from 'chai';
import {Electrodomestico} from '../src/electrodomestico';
import {Lavadora} from '../src/lavadora';
import {Televisor} from '../src/televisor';

const lavadora = new Lavadora(150, 220, "blanco", "A");

describe('TEST LAVADORA', () => {
  it('lavadora not null', () => {
    return expect(lavadora).not.to.be.equal(null);
  });
  it('lavadora color blanco', () => {
    return expect(lavadora.rangoColor()).to.be.equal("blanco");
  });
  it('lavadora con el peso correcto', () => {
    return expect(lavadora.getPeso()).to.be.equal(220);
  });
  it('lavadora con su nuevo precio', () => {
    return expect(lavadora.precioLavadora()).to.be.equal(230);
  });
});
*/
