/*
import {assert} from 'chai';
import {Electrodomestico} from '../src/electrodomestico';

export class Lavadora extends Electrodomestico {
  constructor( protected precio: number, protected peso: number = 5, protected color: string, protected consumo: string) {
    super(precio, peso, color, consumo);
  }

  public precioLavadora() {
    if (this.peso > 100) {
      this.precio += 80;
    }
    return this.precio;
  }
}
*/
